name: Deploy to AKS

on:
  push:
    branches:
      - feature-kubernetes

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v2

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      # Aquí es donde le decimos que construya para ARM64
      - name: Build and push image
        uses: docker/build-push-action@v4
        with:
          context: .
          platforms: linux/arm64
          push: true
          tags: ${{ secrets.DOCKER_USERNAME }}/ms-usuarios:latest

      - name: Azure Login
        uses: azure/login@v1
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: Set AKS Context
        uses: azure/aks-set-context@v3
        with:
          resource-group: ${{ secrets.AZURE_RESOURCE_GROUP }}
          cluster-name: ${{ secrets.AZURE_CLUSTER_NAME }}

      - name: Deploy to AKS
        run: |
          sed -i "s|\${DOCKER_IMAGE}|${{ secrets.DOCKER_USERNAME }}/ms-usuarios:latest|g" k8s/app.yaml
          kubectl apply -f k8s/postgres.yaml
          kubectl apply -f k8s/app.yaml
          # Forzamos el reinicio para que descargue la imagen nueva
          kubectl rollout restart deployment ms-usuarios-app